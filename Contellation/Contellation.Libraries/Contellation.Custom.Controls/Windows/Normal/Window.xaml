<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Contellation.Custom.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Contellation.Custom;component/Generic/Style.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <!--
        SingleBorderWindow preserves the animations and scaling properly.
        By default, the navigation buttons will be hidden by the background.
        If we use Mica, we hide them manually.
    -->
    <Style x:Key="DefaultWindowStyle" TargetType="{x:Type Window}">
        <!--<Setter Property="Background" Value="{DynamicResource WindowBackground}" />-->
        <!--<Setter Property="Foreground" Value="{DynamicResource WindowForeground}" />-->
        <Setter Property="Height" Value="600" />
        <Setter Property="MinHeight" Value="320" />
        <Setter Property="Width" Value="1100" />
        <Setter Property="MinWidth" Value="460" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <!--<Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />-->
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="WindowStyle" Value="SingleBorderWindow" />
        <Setter Property="AllowsTransparency" Value="False" />
        <Setter Property="ResizeMode" Value="CanResize" />
        <!--  The Display option casues a large aliasing effect  -->
        <!-- <Setter Property="TextOptions.TextFormattingMode" Value="Ideal" /> -->
        <!--  I don't know if this is always the case, but ClearType blurs the fonts a bit for me  -->
        <!--<Setter Property="RenderOptions.ClearTypeHint" Value="Enabled" />-->
        <Setter Property="RenderOptions.BitmapScalingMode" Value="Linear" />
        <!--  Aliased breaks rounded control elements such as buttons  -->
        <Setter Property="RenderOptions.EdgeMode" Value="Unspecified" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Window}">
                    <AdornerDecorator>
                        <controls:ClientArea
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter x:Name="ContentPresenter" />
                        </controls:ClientArea>
                    </AdornerDecorator>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="WindowState" Value="Normal">
                <Setter Property="ResizeMode" Value="CanResize" />
            </Trigger>
            <Trigger Property="WindowState" Value="Maximized">
                <Setter Property="ResizeMode" Value="NoResize" />
                <Setter Property="Topmost" Value="False" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style BasedOn="{StaticResource DefaultWindowStyle}" TargetType="{x:Type Window}" />

    <ControlTemplate x:Key="WindowWin10Template" TargetType="{x:Type controls:Window}">
        <Border SnapsToDevicePixels="True" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
            <Grid Margin="{TemplateBinding Padding}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid Background="#d3e3fd">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <!--<controls:TitleBar BorderThickness="0" BorderBrush="Transparent" Background="Violet" Margin="5 0 0 0" Grid.Column="0" Title="{TemplateBinding Title}"/>-->
                    <!--<StackPanel Name="ButtonPanel" WindowChrome.IsHitTestVisibleInChrome="True" UseLayoutRounding="True" VerticalAlignment="Top" Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal">
                        <controls:Button x:Name="PART_MinimizeButton" ActionButton="Minimize"/>
                        <controls:Button x:Name="PART_MaximizeButton" ActionButton="Maximize"/>
                        <controls:Button x:Name="PART_CloseButton" ActionButton="Close"/>
                    </StackPanel>-->
                    <!--<Grid Grid.Column="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Button BorderThickness="0" BorderBrush="Transparent" Name="PART_Icon" Background="Transparent" Margin="5 0 0 0" WindowChrome.IsHitTestVisibleInChrome="True" VerticalAlignment="Center">

                            -->
                    <!--<interactivity:Interaction.Triggers>
                                <interactivity:EventTrigger EventName="MouseDoubleClick">
                                    <interactivity:InvokeCommandAction Command="{x:Static SystemCommands.CloseWindowCommand}" />
                                </interactivity:EventTrigger>
                            </interactivity:Interaction.Triggers>-->
                    <!--

                            <Image IsHitTestVisible="False" Source="{TemplateBinding Icon}" VerticalAlignment="Center" Width="16" Height="16"/>
                        </Button>
                        <TextBlock Grid.Column="1" Text="{TemplateBinding Title}" Padding="10,0,0,0" VerticalAlignment="Center"/>

                        <ContentPresenter WindowChrome.IsHitTestVisibleInChrome="True" Grid.Column="2" x:Name="ContentPresenterMain"/>
                    </Grid>-->
                </Grid>
                <AdornerDecorator Grid.Row="1">
                    <ContentPresenter ClipToBounds="True"/>
                </AdornerDecorator>
                <ResizeGrip Width="10" Height="10" x:Name="ResizeGrip" HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="1" IsTabStop="False" Visibility="Collapsed" WindowChrome.ResizeGripDirection="BottomRight" />
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="ResizeMode" Value="CanResizeWithGrip"/>
                    <Condition Property="WindowState" Value="Normal" />
                </MultiTrigger.Conditions>
                <Setter TargetName="ResizeGrip" Property="Visibility" Value="Visible" />
            </MultiTrigger>
            <!--<Trigger Property="Icon" Value="{x:Null}">
                <Setter TargetName="PART_Icon" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="PART_Icon" Property="Margin" Value="0" />
            </Trigger>-->
            <!--<Trigger Property="IsActive" Value="False">
                <Setter Property="Opacity" Value=".8" TargetName="ButtonPanel"/>
            </Trigger>
            <Trigger Property="WindowState" Value="Normal">
                <Setter Property="Visibility" Value="Visible" TargetName="PART_MaximizeButton"/>
            </Trigger>
            <Trigger Property="ResizeMode" Value="NoResize">
                <Setter Property="Visibility" Value="Collapsed" TargetName="PART_MinimizeButton"/>
                <Setter Property="Visibility" Value="Collapsed" TargetName="PART_MaximizeButton"/>
            </Trigger>
            <Trigger Property="ResizeMode" Value="CanMinimize">
                <Setter Property="Visibility" Value="Collapsed" TargetName="PART_MaximizeButton"/>
            </Trigger>
            <Trigger Property="IsMaximized" Value="True">
                <Setter Property="Icon" Value="&#xf2d2;" TargetName="PART_MaximizeButton"/>
            </Trigger>
            <Trigger Property="ShowMinimize" Value="False">
                <Setter Property="Visibility" Value="Hidden" TargetName="PART_MinimizeButton"/>
            </Trigger>
            <Trigger Property="ShowMaximize" Value="False">
                <Setter Property="Visibility" Value="Hidden" TargetName="PART_MaximizeButton"/>
            </Trigger>
            <Trigger Property="ShowClose" Value="False">
                <Setter Property="Visibility" Value="Hidden" TargetName="PART_CloseButton"/>
            </Trigger>-->
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="DefaultCustomWindowStyle" BasedOn="{StaticResource DefaultWindowStyle}"
        TargetType="{x:Type controls:Window}">
        <Setter Property="Background" Value="#FFFFFF" />
        <!--<Setter Property="Foreground" Value="{DynamicResource WindowForeground}" />-->
        <Setter Property="BorderBrush" Value="#262e2f" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="600" />
        <Setter Property="MinHeight" Value="320" />
        <Setter Property="Width" Value="1100" />
        <Setter Property="MinWidth" Value="460" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Padding" Value="0" />
        <!--<Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />-->
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template" Value="{StaticResource WindowWin10Template}"/>
    </Style>

    <Style BasedOn="{StaticResource DefaultCustomWindowStyle}" TargetType="{x:Type controls:Window}" />

</ResourceDictionary>
