<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Contellation.Custom.Controls"
                    xmlns:enums="clr-namespace:Contellation.Custom.Enums">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Contellation.Custom;component/Generic/Style.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type controls:TitleBar}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource ButtonForeground}" />
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="Padding" Value="16,4,16,4" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Height" Value="48" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:TitleBar}">
                    <Grid x:Name="PART_MainGrid" Background="{TemplateBinding Background}"
                          HorizontalAlignment="{TemplateBinding HorizontalAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Grid x:Name="TitleGrid" Grid.Column="0"
                              HorizontalAlignment="Left" VerticalAlignment="Center"
                              Margin="{TemplateBinding Padding}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!--Biểu tượng ứng dụng tùy chỉnh-->  
                            <ContentPresenter x:Name="PART_Icon" Grid.Column="0" Height="16" Margin="0,0,12,0" 
                                              Focusable="False" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality" 
                                              Content="{TemplateBinding Icon}"/>

                            <!--Tiêu đề ứng dụng chính-->  
                            <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                       FontSize="{TemplateBinding FontSize}" Text="{TemplateBinding Title}" />
                        </Grid>
                        <Grid Grid.Column="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!--Nội dung tiêu đề bổ sung-->  
                            <ContentPresenter Grid.Column="0" HorizontalAlignment="Right"
                                              Content="{TemplateBinding Header}" />

                            <!--Các nút điều hướng - Đóng, Khôi phục, Phóng to, Thu nhỏ-->  
                            <Grid Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <controls:Button x:Name="PART_HelpButton"
                                                 Grid.Column="0" TypeAction="Help" />

                                <controls:Button x:Name="PART_MinimizeButton"
                                                 Grid.Column="1" TypeAction="Minimize"  />

                                <controls:Button x:Name="PART_MaximizeButton"
                                                 Grid.Column="2" TypeAction="Maximize" />

                                <controls:Button x:Name="PART_CloseButton"
                                                 Grid.Column="4" TypeAction="Close" />
                            </Grid>
                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Icon" Value="{x:Null}" />
                                <Condition Property="Title" Value="{x:Null}" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="TitleGrid" Property="Visibility" Value="Collapsed" />
                        </MultiTrigger>
                        <Trigger Property="Icon" Value="{x:Null}">
                            <Setter TargetName="PART_Icon" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="PART_Icon" Property="Margin" Value="0" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="ShowMaximize" Value="True" />
                                <Condition Property="IsMaximized" Value="True" />
                            </MultiTrigger.Conditions>

                            <Setter TargetName="PART_MaximizeButton" Property="TypeAction" Value="Restore" />
                        </MultiTrigger>
                        <Trigger Property="ShowHelp" Value="False">
                            <Setter TargetName="PART_HelpButton" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="ShowMinimize" Value="False">
                            <Setter TargetName="PART_MinimizeButton" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="ShowMaximize" Value="False">
                            <Setter TargetName="PART_MaximizeButton" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="ShowClose" Value="False">
                            <Setter TargetName="PART_CloseButton" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
