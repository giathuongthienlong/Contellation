<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Contellation.Custom.Controls"
                    xmlns:enums="clr-namespace:Contellation.Custom.Enums">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Contellation.Custom;component/Generic/Style.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Thickness x:Key="ButtonPadding">11,5,11,6</Thickness>
    <Thickness x:Key="ButtonBorderThemeThickness">1</Thickness>
    <Thickness x:Key="ButtonIconMargin">0,0,8,0</Thickness>

    <Style x:Key="DefaultRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
        <Setter Property="FocusVisualStyle" Value="{DynamicResource DefaultControlFocusVisualStyle}" />
        <Setter Property="Background" Value="{DynamicResource BackgroundBrushShare}" />
        <Setter Property="Foreground" Value="{DynamicResource Col_2B2B2B}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Col_2B2B2B}" />
        <Setter Property="BorderThickness" Value="{StaticResource DefaultThickness}" />
        <Setter Property="Padding" Value="{StaticResource DefaultPadding}" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="FontSize" Value="{DynamicResource DefaultFontSize}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Border.CornerRadius" Value="{DynamicResource DefaultCornerRadius}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border x:Name="ContentBorder" Padding="{TemplateBinding Padding}"
                            MinWidth="{TemplateBinding MinWidth}" Width="{TemplateBinding Width}"
                            MinHeight="{TemplateBinding MinHeight}" Height="{TemplateBinding Height}"
                            Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding Border.CornerRadius}"
                            HorizontalAlignment="{TemplateBinding HorizontalAlignment}" VerticalAlignment="{TemplateBinding VerticalAlignment}">
                        <ContentPresenter x:Name="ContentPresenter" RecognizesAccessKey="True" Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}" TextElement.Foreground="{TemplateBinding Foreground}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsPressed" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource Col_A6A6A6}" />
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="{DynamicResource Col_2B2B2B}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsPressed" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource Col_FFFFFF}" />
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="{DynamicResource Col_2B2B2B}" />
                            <Setter TargetName="ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource Col_A6A6A6}" />
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource Col_2B2B2B}" />
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="{DynamicResource BackgroundBrushShare}" />
                            <Setter TargetName="ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource Col_A6A6A6}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DefaultRepeatButtonStyle}" TargetType="{x:Type RepeatButton}" />

    <Style x:Key="DefaultButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="FocusVisualStyle" Value="{DynamicResource DefaultControlFocusVisualStyle}" />
        <Setter Property="Background" Value="{DynamicResource BackgroundBrushShare}" />
        <Setter Property="Foreground" Value="{DynamicResource Col_2B2B2B}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Col_2B2B2B}" />
        <Setter Property="BorderThickness" Value="{StaticResource DefaultThickness}" />
        <Setter Property="Padding" Value="{StaticResource DefaultPadding}" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FontSize" Value="{DynamicResource DefaultFontSize}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Border.CornerRadius" Value="{DynamicResource DefaultCornerRadius}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="ContentBorder" Padding="{TemplateBinding Padding}"
                            MinWidth="{TemplateBinding MinWidth}" Width="{TemplateBinding Width}"
                            MinHeight="{TemplateBinding MinHeight}" Height="{TemplateBinding Height}"
                            Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding Border.CornerRadius}"
                            HorizontalAlignment="{TemplateBinding HorizontalAlignment}" VerticalAlignment="{TemplateBinding VerticalAlignment}">
                        <ContentPresenter x:Name="ContentPresenter" RecognizesAccessKey="True" Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}" TextElement.Foreground="{TemplateBinding Foreground}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        <!--
                       Without the hacky workaround for the themes, we cannot use the freezable VisualState.

                       <VisualStateManager.VisualStateGroups>
                           <VisualStateGroup x:Name="CommonStates">
                               <VisualState x:Name="Normal" />
                               <VisualState x:Name="MouseOver">
                                   <Storyboard>
                                      <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentBorder" Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{DynamicResource ButtonBackgroundPointerOver}" />
                                        </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentBorder" Storyboard.TargetProperty="BorderBrush">
                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{DynamicResource ControlElevationBorderBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="(TextElement.Foreground)">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{DynamicResource ButtonForegroundPointerOver}" />
                                        </ObjectAnimationUsingKeyFrames>
                                   </Storyboard>
                               </VisualState>
                               <VisualState x:Name="Pressed">
                                   <Storyboard>
                                       <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentBorder" Storyboard.TargetProperty="Background">
                                           <DiscreteObjectKeyFrame KeyTime="0" Value="{DynamicResource ButtonBackgroundPressed}" />
                                       </ObjectAnimationUsingKeyFrames>
                                       <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentBorder" Storyboard.TargetProperty="BorderBrush">
                                           <DiscreteObjectKeyFrame KeyTime="0" Value="{DynamicResource ButtonBorderBrushPressed}" />
                                       </ObjectAnimationUsingKeyFrames>
                                       <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="(TextElement.Foreground)">
                                           <DiscreteObjectKeyFrame KeyTime="0" Value="{DynamicResource ButtonForegroundPressed}" />
                                       </ObjectAnimationUsingKeyFrames>
                                   </Storyboard>
                               </VisualState>
                               <VisualState x:Name="Disabled">
                                   <Storyboard>
                                       <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentBorder" Storyboard.TargetProperty="Background">
                                           <DiscreteObjectKeyFrame KeyTime="0" Value="{DynamicResource ButtonBackgroundDisabled}" />
                                       </ObjectAnimationUsingKeyFrames>
                                       <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentBorder" Storyboard.TargetProperty="BorderBrush">
                                           <DiscreteObjectKeyFrame KeyTime="0" Value="{DynamicResource ButtonBorderBrushDisabled}" />
                                       </ObjectAnimationUsingKeyFrames>
                                       <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="(TextElement.Foreground)">
                                           <DiscreteObjectKeyFrame KeyTime="0" Value="{DynamicResource ButtonForegroundDisabled}" />
                                       </ObjectAnimationUsingKeyFrames>
                                   </Storyboard>
                               </VisualState>
                           </VisualStateGroup>
                       </VisualStateManager.VisualStateGroups>
                       -->
                        
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsPressed" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource Col_A6A6A6}" />
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="{DynamicResource Col_2B2B2B}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsPressed" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource Col_2B2B2B}" />
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="{DynamicResource BackgroundBrushShare}" />
                            <Setter TargetName="ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource Col_A6A6A6}" />
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource Col_2B2B2B}" />
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="{DynamicResource BackgroundBrushShare}" />
                            <Setter TargetName="ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource Col_A6A6A6}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DefaultButtonStyle}" TargetType="{x:Type Button}" />

    <Style x:Key="DefaultUiButtonStyle" TargetType="{x:Type controls:Button}">
        <Setter Property="FocusVisualStyle" Value="{DynamicResource DefaultControlFocusVisualStyle}" />
        <Setter Property="Background" Value="{DynamicResource BackgroundBrushShare}" />
        <Setter Property="MouseOverBackground" Value="{DynamicResource Col_2B2B2B}" />
        <Setter Property="PressedBackground" Value="{DynamicResource Col_2B2B2B}" />
        <Setter Property="Foreground" Value="{DynamicResource Col_2B2B2B}" />
        <Setter Property="PressedForeground" Value="{DynamicResource Col_2B2B2B}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Col_2B2B2B}" />
        <Setter Property="PressedBorderBrush" Value="{DynamicResource Col_2B2B2B}" />
        <Setter Property="MouseOverBorderBrush" Value="{DynamicResource Col_2B2B2B}" />
        <Setter Property="BorderThickness" Value="{StaticResource DefaultThickness}" />
        <Setter Property="Padding" Value="{StaticResource DefaultPadding}" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FontSize" Value="{DynamicResource DefaultFontSize}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="CornerRadius" Value="{DynamicResource DefaultCornerRadius}" />
        <Setter Property="Appearance" Value="Secondary" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Button}">
                    <Border x:Name="ContentBorder" BorderThickness="0"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            MinWidth="{TemplateBinding MinWidth}" Width="{TemplateBinding Width}"
                            MinHeight="{TemplateBinding MinHeight}" Height="{TemplateBinding Height}"
                            Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                            HorizontalAlignment="{TemplateBinding HorizontalAlignment}" VerticalAlignment="{TemplateBinding VerticalAlignment}">
                        <!--  
                            Viền lõm chỉ được sử dụng cho các giao diện không phải là Giao diện phụ.  
                        -->
                        <Border x:Name="InsetBorder"
                                Padding="{TemplateBinding Padding}"
                                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                BorderBrush="{Binding ElementName=ContentBorder, Path=BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding CornerRadius}">
                            <Grid HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- 
                                    Biểu tượng và nội dung được đặt trong cùng một lưới để chúng có thể chia sẻ cùng trạng thái khi di chuột và khi nhấn, 
                                    đảm bảo rằng biểu tượng cũng thay đổi màu sắc khi nút được di chuột hoặc nhấn vào.
                                -->
                                <ContentPresenter x:Name="ControlIcon" Grid.Column="0"
                                                  VerticalAlignment="Center" Focusable="False" Margin="{StaticResource ButtonIconMargin}"
                                                  Content="{Binding Path=(controls:Element.Icon), RelativeSource={RelativeSource TemplatedParent}}"
                                                  TextElement.FontFamily="{Binding (controls:Element.FontFamilyIcon), RelativeSource={RelativeSource TemplatedParent}}"
                                                  TextElement.FontSize="{TemplateBinding FontSize}" TextElement.Foreground="{TemplateBinding Foreground}" />

                                <ContentPresenter x:Name="ContentPresenter" Grid.Column="1" 
                                                  VerticalAlignment="Center" RecognizesAccessKey="True" Content="{TemplateBinding Content}" 
                                                  TextElement.Foreground="{TemplateBinding Foreground}" ContentTemplate="{TemplateBinding ContentTemplate}"/>
                            </Grid>
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!--  TRANSPARENT  -->
                        <Trigger Property="Appearance" Value="Transparent">
                            <Setter Property="Background" Value="{DynamicResource BackgroundBrushShare}" />
                        </Trigger>

                        <!--  PRIMARY  -->
                        <Trigger Property="Appearance" Value="Primary">
                            <Setter Property="Background" Value="{DynamicResource Col_D6B4FD}" />
                            <Setter Property="MouseOverBackground" Value="{DynamicResource Col_FFEE32}" />
                            <Setter Property="PressedBackground" Value="{DynamicResource Col_D6B4FD}" />
                            <Setter Property="Foreground" Value="{DynamicResource Col_2B2B2B}" />
                            <Setter Property="PressedForeground" Value="{DynamicResource Col_000000}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource Col_FFFFFF}" />
                            <Setter Property="MouseOverBorderBrush" Value="{DynamicResource Col_FFFFFF}" />
                            <Setter Property="PressedBorderBrush" Value="{DynamicResource BackgroundBrushShare}" />
                        </Trigger>

                        <!--  SECONDARY  -->
                        <Trigger Property="Appearance" Value="Secondary">
                            <Setter TargetName="InsetBorder" Property="BorderThickness" Value="0" />
                            <Setter TargetName="ContentBorder" Property="BorderThickness" Value="{Binding BorderThickness, RelativeSource={RelativeSource TemplatedParent}}" />
                        </Trigger>

                        <!--  DARK  -->
                        <Trigger Property="Appearance" Value="Dark">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{DynamicResource Col_72000000}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="MouseOverBackground">
                                <Setter.Value>
                                    <SolidColorBrush Color="{DynamicResource Col_62000000}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="PressedBackground">
                                <Setter.Value>
                                    <SolidColorBrush Color="{DynamicResource Col_52000000}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="PressedBorderBrush" Value="Transparent" />
                            <Setter Property="Foreground" Value="{DynamicResource Col_FFFFFF}" />
                            <Setter Property="PressedForeground" Value="{DynamicResource Col_C5FFFFFF}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource Col_FFFFFF}" />
                            <Setter Property="MouseOverBorderBrush" Value="{DynamicResource Col_FFFFFF}" />
                        </Trigger>

                        <!--  LIGHT  -->
                        <Trigger Property="Appearance" Value="Light">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{DynamicResource Col_B3FFFFFF}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="MouseOverBackground">
                                <Setter.Value>
                                    <SolidColorBrush Color="{DynamicResource Col_F3FFFFFF}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="PressedBackground">
                                <Setter.Value>
                                    <SolidColorBrush Color="{DynamicResource Col_F3FFFFFF}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Foreground" Value="{DynamicResource Col_2B2B2B}" />
                            <Setter Property="PressedForeground" Value="{DynamicResource Col_BE000000}" />
                        </Trigger>

                        <!--  INFO  -->
                        <Trigger Property="Appearance" Value="Info">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{DynamicResource Col_03A9F4}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="MouseOverBackground">
                                <Setter.Value>
                                    <SolidColorBrush Opacity="0.9" Color="{DynamicResource Col_03A9F4}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="PressedBackground">
                                <Setter.Value>
                                    <SolidColorBrush Opacity="0.7" Color="{DynamicResource Col_03A9F4}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Foreground" Value="{DynamicResource Col_2B2B2B}" />
                            <Setter Property="PressedBorderBrush" Value="Transparent" />
                            <Setter Property="BorderBrush" Value="{DynamicResource Col_FFFFFF}" />
                            <Setter Property="MouseOverBorderBrush" Value="{DynamicResource Col_FFFFFF}" />
                        </Trigger>

                        <!--  DANGER  -->
                        <Trigger Property="Appearance" Value="Danger">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{DynamicResource Col_F44336}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="MouseOverBackground">
                                <Setter.Value>
                                    <SolidColorBrush Opacity="0.9" Color="{DynamicResource Col_F44336}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="PressedBackground">
                                <Setter.Value>
                                    <SolidColorBrush Opacity="0.7" Color="{DynamicResource Col_F44336}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="PressedBorderBrush" Value="Transparent" />
                            <Setter Property="BorderBrush" Value="{DynamicResource Col_FFFFFF}" />
                            <Setter Property="MouseOverBorderBrush" Value="{DynamicResource Col_FFFFFF}" />
                        </Trigger>

                        <!--  SUCCESS  -->
                        <Trigger Property="Appearance" Value="Success">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{DynamicResource Col_4CAF50}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="MouseOverBackground">
                                <Setter.Value>
                                    <SolidColorBrush Opacity="0.9" Color="{DynamicResource Col_4CAF50}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="PressedBackground">
                                <Setter.Value>
                                    <SolidColorBrush Opacity="0.7" Color="{DynamicResource Col_4CAF50}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="PressedBorderBrush" Value="Transparent" />
                            <Setter Property="BorderBrush" Value="{DynamicResource Col_FFFFFF}" />
                            <Setter Property="MouseOverBorderBrush" Value="{DynamicResource Col_FFFFFF}" />
                        </Trigger>

                        <!--  CAUTION  -->
                        <Trigger Property="Appearance" Value="Caution">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{DynamicResource Col_FF9800}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="MouseOverBackground">
                                <Setter.Value>
                                    <SolidColorBrush Opacity="0.9" Color="{DynamicResource Col_FF9800}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="PressedBackground">
                                <Setter.Value>
                                    <SolidColorBrush Opacity="0.7" Color="{DynamicResource Col_FF9800}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="PressedBorderBrush" Value="Transparent" />
                            <Setter Property="BorderBrush" Value="{DynamicResource Col_FFFFFF}" />
                            <Setter Property="MouseOverBorderBrush" Value="{DynamicResource Col_FFFFFF}" />
                        </Trigger>

                        <Trigger Property="TypeAction" Value="{x:Static enums:ButtonAction.Minimize}">
                            <Setter Property="Content" Value="O" />
                            <Setter Property="Width" Value="44" />
                            <Setter Property="Height" Value="30" />
                            <Setter Property="Background" Value="{DynamicResource BackgroundBrushShare}" />
                            <Setter Property="Command" Value="{Binding Path=TemplateButtonCommand, RelativeSource={RelativeSource AncestorType={x:Type controls:TitleBar}}}" />
                            <Setter Property="CommandParameter" Value="{x:Static enums:ButtonAction.Minimize}" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="CornerRadius" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="Width" Value="48" />
                            <Setter Property="ToolTip" Value="Minimize" />
                            <Setter Property="MouseOverButtonsForeground" Value="Gray" />
                        </Trigger>

                        <Trigger Property="TypeAction" Value="{x:Static enums:ButtonAction.Restore}">
                            <Setter Property="Content" Value="O" />
                            <Setter Property="Width" Value="44" />
                            <Setter Property="Height" Value="30" />
                            <Setter Property="Background" Value="{DynamicResource BackgroundBrushShare}" />
                            <Setter Property="Command" Value="{Binding Path=TemplateButtonCommand, RelativeSource={RelativeSource AncestorType={x:Type controls:TitleBar}}}" />
                            <Setter Property="CommandParameter" Value="{x:Static enums:ButtonAction.Restore}" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="CornerRadius" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="Width" Value="48" />
                            <Setter Property="ToolTip" Value="Restore" />
                            <Setter Property="MouseOverButtonsForeground" Value="Gray" />
                        </Trigger>
                        
                        <Trigger Property="TypeAction" Value="{x:Static enums:ButtonAction.Maximize}">
                            <Setter Property="Content" Value="O" />
                            <Setter Property="Width" Value="44" />
                            <Setter Property="Height" Value="30" />
                            <Setter Property="Background" Value="{DynamicResource BackgroundBrushShare}" />
                            <Setter Property="Command" Value="{Binding Path=TemplateButtonCommand, RelativeSource={RelativeSource AncestorType={x:Type controls:TitleBar}}}" />
                            <Setter Property="CommandParameter" Value="{x:Static enums:ButtonAction.Maximize}" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="CornerRadius" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="Width" Value="48" />
                            <Setter Property="ToolTip" Value="Maximize" />
                            <Setter Property="MouseOverButtonsForeground" Value="Gray" />
                        </Trigger>

                        <Trigger Property="TypeAction" Value="{x:Static enums:ButtonAction.Close}">
                            <Setter Property="Content" Value="X" />
                            <Setter Property="Width" Value="44" />
                            <Setter Property="Height" Value="30" />
                            <Setter Property="Background" Value="{DynamicResource BackgroundBrushShare}" />
                            <Setter Property="Command" Value="{Binding Path=TemplateButtonCommand, RelativeSource={RelativeSource AncestorType={x:Type controls:TitleBar}}}" />
                            <Setter Property="CommandParameter" Value="{x:Static enums:ButtonAction.Close}" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="CornerRadius" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="Width" Value="48" />
                            <Setter Property="ToolTip" Value="Close" />
                            <Setter Property="MouseOverButtonsForeground" Value="Gray" />
                            <Setter Property="MouseOverBackground" Value="#F44336" />
                        </Trigger>

                        <!--  SHARED TRIGGERS  -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsPressed" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentBorder" Property="Background" Value="{Binding MouseOverBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="{Binding MouseOverBorderBrush, RelativeSource={RelativeSource TemplatedParent}}" />
                        </MultiTrigger>
                        
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsPressed" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentBorder" Property="Background" Value="{Binding PressedBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="{Binding PressedBorderBrush, RelativeSource={RelativeSource TemplatedParent}}" />
                            <Setter Property="Foreground" Value="{Binding PressedForeground, RelativeSource={RelativeSource TemplatedParent}}" />
                        </MultiTrigger>

                        <Trigger Property="Content" Value="{x:Null}">
                            <Setter TargetName="ControlIcon" Property="Margin" Value="0" />
                            <Setter TargetName="ControlIcon" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="Content" Value="">
                            <Setter TargetName="ControlIcon" Property="Margin" Value="0" />
                        </Trigger>
                        <Trigger Property="controls:Element.Icon" Value="{x:Null}">
                            <Setter TargetName="ControlIcon" Property="Margin" Value="0" />
                            <Setter TargetName="ControlIcon" Property="Visibility" Value="Collapsed" />
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource Col_000000}" />
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="{DynamicResource BackgroundBrushShare}" />
                            <Setter Property="Foreground" Value="{DynamicResource Col_A6A6A6}" />
                        </Trigger>
                        
                        
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DefaultUiButtonStyle}" TargetType="{x:Type controls:Button}" />


</ResourceDictionary>
